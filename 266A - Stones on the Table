#include<bits/stdc++.h>
using namespace std;
int main()
{
	int n;
	scanf("%d",&n);
	char arr[n];
	/*for(int i=0;i<n;i++)
	{
		scanf("%c",&arr[i]);
	}*/
	scanf("%s",arr);
	int count=0;
	for(int i=0;i<n;i++)
	{
		if(arr[i]==arr[i+1])
		{
			count++;
		}
	}
   printf("%d\n",count);
   return 0;
}
